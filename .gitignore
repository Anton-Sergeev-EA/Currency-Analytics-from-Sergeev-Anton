# Секреты и конфиги
.env
.env.*
# .env.* above would also swallow the tracked, secret-free .env.example
# template (README's Quick Start has always said to `cp .env.example
# .env`, but the file itself was never committed - see .env.example).
!.env.example
*.key
*.pem
*.crt
*.p12
*.jks
*.keystore
*secret*
*password*
*credential*
*private*

# Логи и данные
logs/

# Let's Encrypt HTTP-01 challenge webroot (runtime files only, see
# scripts/init_letsencrypt.sh) and any local self-signed cert leftovers.
certbot-webroot/
ssl/
*.log
/data/
*.db
*.sqlite

# Модели
/models/
*.joblib
*.pkl
*.h5
*.pt
*.pth

# Временные файлы
__pycache__/
*.pyc
*.pyo
*.tmp
*.swp
.idea/
.vscode/
.DS_Store

# Бэкапы
*.bak
*.bak2
*.save
*.backup

# Docker
.venv/
